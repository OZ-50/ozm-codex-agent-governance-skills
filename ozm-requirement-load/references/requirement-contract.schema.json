{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ozm.requirement_contract.v1",
  "type": "object",
  "required": ["source_surfaces", "authority_class", "must_not_do", "reference_intake_required", "fallback_path", "verification_surface", "claim_ceiling_if_missing"],
  "properties": {
    "source_surfaces": {"type": "array", "items": {"type": "string"}},
    "authority_class": {"type": "string"},
    "must_not_do": {"type": "array", "items": {"type": "string"}},
    "reference_intake_required": {"type": "boolean"},
    "fallback_path": {"type": "string"},
    "verification_surface": {"type": "array", "items": {"type": "string"}},
    "claim_ceiling_if_missing": {"type": "string"}
  },
  "additionalProperties": true
}
